; -*- mode: lisp -*-
(in-package #:cl-user)
(defpackage #:<%= (@ project-name ) %>-test/models/<%= (@ name ) %>
  (:use #:cl
        #:rove
        #:clails/test))
(in-package #:<%= (@ project-name ) %>-test/models/<%= (@ name ) %>)

(deftest-suite :model test-<%= (@ name ) %>-model
  (testing "Test <%= (@ name ) %> model"
    (ok (= 1 0) "This test should be replaced with actual test")))
